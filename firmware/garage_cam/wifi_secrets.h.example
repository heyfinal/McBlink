#pragma once
// Copy this file to wifi_secrets.h and fill in your network.
// wifi_secrets.h is gitignored so credentials never reach the repo.
#define WIFI_SSID "YOUR_SSID"
#define WIFI_PASS "YOUR_WIFI_PASS"
